services:
  - type: web
    name: techblogai-backend
    env: node
    rootDir: /server  
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.x
    autoDeploy: true